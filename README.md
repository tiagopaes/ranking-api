# ranking-api
A simple API for game ranking

## Steps to install

- `git clone`
- `cd ranking-api`
- `docker-compose up`
- `docker exec -it ranking-app /bin/bash`
- `cp .env.example .env`
- `php artisan key:generate`
- `composer install`

